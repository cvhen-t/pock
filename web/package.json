{
  "name": "wasteland-stack-cards",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "bake:vfx": "node scripts/bake-attack-vfx.mjs",
    "bake:icons": "npx tsx scripts/bake-card-icons.ts",
    "bake:icon": "npx tsx scripts/bake-card-icons.ts",
    "process:world": "node scripts/process-world-sprite.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "beta"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.67",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
